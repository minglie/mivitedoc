# 借用核心（Rust 借用机制核心）

## 一、基本原则

- 在任意给定时间，要么 **只能有一个可变引用**，要么 **只能有多个不可变引用**。  
- 引用必须始终是有效的。

> Rust 是一个**用栈内存管理堆内存**的设计。

---

## 二、函数与作用域中的借用规则

编写 Rust 函数时要记住：

- 当作用域（函数）结束，运行栈上的数据会**全部被释放**。  
- 如果后续还需要使用这些数据，有两种方式：
  - **返回（转移所有权）**
  - **提前声明是借用（借别人的就得还）**

这就是 Rust **内存管理的精髓**所在。

---

## 三、Rust 中的接收实例方式

在 Rust 中，方法必须明确指定接收实例的方式：

| 形式 | 含义 |
|:----|:----|
| `&a` | 不可变引用 |
| `&mut a` | 可变引用 |
| `a` | 获取所有权 |

下面分别举例说明：

---

### 1. `&a` —— 不可变引用（Immutable Reference）

```rust
struct Book {
    title: String,
}

impl Book {
    fn show_title(&self) {
        // 这里只是“借用”self，不会修改它
        println!("书名：{}", self.title);
    }
}

fn main() {
    let b = Book { title: String::from("Rust 权威指南") };
    b.show_title(); // 传入的是不可变引用 &self
    println!("main 中依然可以使用 b：{}", b.title);
}
```
### 2. &mut a —— 可变引用（Mutable Reference）
```rust
struct Counter {
    value: i32,
}

impl Counter {
    fn increase(&mut self) {
        self.value += 1;
    }
}

fn main() {
    let mut c = Counter { value: 0 };
    c.increase(); // 传入的是可变引用 &mut self
    println!("计数值：{}", c.value);
}
```
✅ 特点：

- 只能存在一个可变引用可以修改原数据内容。；


### 3. a —— 获取所有权（Move Ownership）
```rust
struct User {
    name: String,
}

impl User {
    fn consume(self) {
        println!("用户 {} 被消费（所有权转移）", self.name);
    }
}

fn main() {
    let u = User { name: String::from("Alice") };
    u.consume(); // 所有权被移动进函数
    // println!("{}", u.name); // ❌ 错误：u 的所有权已被移动
}

```
✅ 特点：

- 函数接收完整所有权；

- 调用者不再拥有该变量。

💡 总结

Rust 通过**所有权（Ownership）和借用（Borrowing）**机制，确保：

- 内存安全；

- 数据竞争在编译期被消除；

- 程序无需垃圾回收器也能高效运行。